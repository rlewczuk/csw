{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Loads a skill from .agents/skills and injects its instructions into the conversation context.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The skill name selected from available_skills section in tool description."
    }
  },
  "required": [
    "name"
  ],
  "additionalProperties": false
}
