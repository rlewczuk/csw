{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Fetches content from the web and optionally converts HTML to Markdown.",
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "description": "The URL to fetch. Supports http and https schemes."
    },
    "format": {
      "type": "string",
      "description": "Output format for fetched content. Use 'markdown' to convert HTML to Markdown, or 'raw' to return response body as-is.",
      "enum": [
        "markdown",
        "raw"
      ]
    },
    "timeout": {
      "type": "integer",
      "description": "Request timeout in seconds. If not specified, defaults to 30 seconds."
    }
  },
  "required": [
    "url",
    "format"
  ],
  "additionalProperties": false
}
