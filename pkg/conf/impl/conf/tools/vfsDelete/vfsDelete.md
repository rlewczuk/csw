Deletes a file or directory from the local filesystem.

Usage:
- The path parameter must be an absolute path, not a relative path
- This tool will permanently delete the file or directory at the specified path
- Use with caution as deleted files cannot be recovered
- If the path is a directory, it must be empty before deletion
- No masks are supported, you must use exact path to given file or directory
