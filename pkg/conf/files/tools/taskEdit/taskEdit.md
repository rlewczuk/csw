Edits task prompt content (`task.md`) by replacing exact text fragments.

Task can be identified by:
- `uuid`, or
- `name`, or
- current session task when neither is provided.

Use this tool to apply focused prompt updates with exact string replacement semantics.
