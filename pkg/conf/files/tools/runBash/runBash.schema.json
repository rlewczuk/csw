{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Executes a bash command in the project directory and returns the output.",
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "description": "The bash command to execute."
    },
    "workdir": {
      "type": "string",
      "description": "Working directory to run the command in. If not specified, project root is used. If relative, it is relative to project root. If absolute, it is used as is but command will require permission from user."
    },
    "timeout": {
      "type": "integer",
      "description": "Timeout in seconds. If not specified, default timeout is used."
    },
      "limit": {
        "type": "integer",
        "description": "Maximum number of lines to return in the output. If not specified, default is 500 lines. Use 0 for no limit."
      }
  },
  "required": ["command"],
  "additionalProperties": false
}
