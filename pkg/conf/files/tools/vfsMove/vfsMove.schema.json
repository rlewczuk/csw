{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Moves or renames a file or directory from the source path to the destination path.",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "The absolute path to the source file or directory to move."
    },
    "destination": {
      "type": "string",
      "description": "The absolute path to the destination where the file or directory should be moved."
    }
  },
  "required": ["path", "destination"],
  "additionalProperties": false
}
