{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Returns task metadata, optionally last session summary, or task prompt content when promptOnly is enabled.",
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string",
      "description": "Task UUID. Optional, use if you look for specific task."
    },
    "name": {
      "type": "string",
      "description": "Task name/slug. Optional parameter, use if you look for specific task."
    },
    "summary": {
      "type": "boolean",
      "description": "If true, include last session summary (metadata and markdown summary text)."
    },
    "promptOnly": {
      "description": "If true, returns task prompt content.",
      "type": "boolean"
    }
  },
  "required": [],
  "additionalProperties": false
}
