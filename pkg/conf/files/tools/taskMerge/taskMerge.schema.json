{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Marks task as finished by merging feature branch into parent branch.",
  "type": "object",
  "properties": {
    "uuid": {
      "type": "string",
      "description": "Task UUID. Optional if name is provided or current session belongs to a task."
    },
    "name": {
      "type": "string",
      "description": "Task name/slug used when uuid is omitted."
    }
  },
  "required": [],
  "additionalProperties": false
}
