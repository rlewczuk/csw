# Package `pkg/ui/cli/testfixture` Overview

Package `pkg/ui/cli/testfixture` provides CLI integration test fixtures for setting up test environments with SweSystem and presenter fixtures.

## Important files

* `fixture.go` - CLI test fixture with SweSystem setup

## Important public API objects

* `CliFixture` - Shared setup for CLI integration tests
* `NewCliFixture` - Creates CLI fixture with SweSystem
